import {
    MODULE,
    MODULE_TYPE_DARK,
} from '../consts/generalConsts';

const Modules = [
    {
        name: MODULE,
        type: MODULE_TYPE_DARK,
        path: '',
        icon: 'home',
        caption: 'modules.home',
    },
];

export default Modules;
